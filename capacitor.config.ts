import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fm-challenges-ionic-product-feedback',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
